# [XAS-006] Phase 2 — Rename marketplace registry and update manifest descriptions

**Status**: Open
**Priority**: High
**Epic**: XAS-002
**Component**: .claude-plugin/plugin.json, .claude-plugin/marketplace.json

**Summary**:
As a user installing from the marketplace, I want the marketplace registry to be named `xxthunder-agentic-skills` and manifest descriptions to reflect the marketplace purpose so that `claude plugin marketplace list` and related UIs show consistent naming.

**Description**:
Phase 2 of the rename epic. Lands together with XAS-007 (README rewrite) and XAS-014 (folder restructure). Changes the marketplace registry name and updates descriptions; plugin name is deliberately unchanged.

Changes:
- `marketplace.json.name`: `xxthunder-skills` → `xxthunder-agentic-skills`
- `marketplace.json.description`: reframe as an agentic skill marketplace
- `plugin.json.description`: review and update if positioning changes (scope: the dev-skills plugin specifically, not the marketplace)

**Scope Decisions**:
- Plugin name remains `xxthunder-dev-skills`.
- Marketplace registry name aligns with repo name.
- `marketplace.json.plugins[0].source` path update is handled by XAS-014 (folder restructure), not here.

**Acceptance Criteria**:
- [ ] `marketplace.json.name` updated
- [ ] `marketplace.json.description` updated to marketplace framing
- [ ] `plugin.json.description` reviewed and updated if needed
- [ ] `plugin.json.name` unchanged
