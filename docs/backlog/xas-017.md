# [XAS-017] Migrate `simplex-merge` from user-global skills into the plugin

**Status**: Open
**Priority**: High
**Epic**: XAS-015
**Component**: plugins/xxthunder-paperless-skills/skills/simplex-merge/, ~/.claude/skills/simplex-merge/

**Summary**:
As a user, I want `simplex-merge` to live in the marketplace plugin so that it's versioned, shared, and installed consistently alongside the other paperless skills.

**Description**:
Move (not copy) the existing `~/.claude/skills/simplex-merge/SKILL.md` into `plugins/xxthunder-paperless-skills/skills/simplex-merge/SKILL.md`. Add the standard `<!-- Source: ... -->` header pointing at the new GitHub path. Once the plugin install is verified locally, delete the global skill file so there's no drift.

**Scope Decisions**:
- Move (not copy): no parallel copies of the skill; avoids drift.
- Trigger phrases and skill content stay unchanged at migration time — content evolution is out of scope for this story.

**Acceptance Criteria**:
- [ ] `SKILL.md` present at `plugins/xxthunder-paperless-skills/skills/simplex-merge/`
- [ ] `Source:` header added to the migrated file
- [ ] Skill trigger phrases unchanged from the original
- [ ] Plugin installed locally and skill triggers end-to-end with a real scan pair
- [ ] `~/.claude/skills/simplex-merge/` deleted after verification
