# [XAS-015] Launch `xxthunder-paperless-skills` plugin

**Status**: In Progress
**Priority**: High
**Component**: plugins/xxthunder-paperless-skills/, .claude-plugin/marketplace.json

**Summary**:
As a paperless-office builder, I want a dedicated plugin bundling scanning, merging, and filing skills so that I can digitize and sort household paperwork (family, house, car, etc.) with minimal friction.

**Description**:
Second plugin in the marketplace, dedicated to digitizing and managing physical paperwork. Seed with three skills: `simplex-merge` (already authored as a user-global skill, migrated here), `naps2-scan` (new — end-to-end scan pipeline), and `auto-file` (new — sort digitized documents into a configured folder structure).

**Scope Decisions**:
- **Plugin name**: `xxthunder-paperless-skills`.
- **Initial skills (3)**: `simplex-merge`, `naps2-scan`, `auto-file`.
- **Simplex-merge migration**: move, not copy — the global `~/.claude/skills/simplex-merge/` is deleted once the plugin install is verified. No parallel copies.
- **NAPS2 skill scope**: end-to-end pipeline — scan via NAPS2.Console.exe, OCR (Tesseract), chain into `simplex-merge` when input is two simplex halves, propose a content-derived filename.
- **Auto-file skill**: sorts into a given root folder with a user-configurable subfolder structure; classification is content-driven (based on OCR text / metadata).
- **Target OS**: Windows (NAPS2 is Windows-native).

**Substories**:
- XAS-015a — Add `auto-file` skill (sort into configurable folder structure)
- XAS-015b — Add `split-batch` skill (detect + split multi-document PDFs)
- XAS-015c — Unit test harness + backfill for plugin Python scripts
- XAS-015d — Privacy opt-in for LLM content analysis in naps2-scan and split-batch
- XAS-015e — Harden split-batch page-number rule: detect self-contained pages

**Legacy substories** (created before the letter-suffix convention; kept at bare IDs):
- XAS-016 — Scaffold `xxthunder-paperless-skills` plugin and register in marketplace (Done)
- XAS-017 — Migrate `simplex-merge` from user-global skills into the plugin (Done)
- XAS-018 — Add `naps2-scan` skill (scan → OCR → merge → filename proposal) (Done)
- XAS-020 — Document `ExcludeBlankPages` profile option in naps2-scan (Done)
- XAS-021 — Persist naps2-scan session settings across invocations (Done)
- XAS-022 — Migrate simplex-merge to uv-run script with PEP 723 deps (Done)
- XAS-023 — Fix skill trigger descriptions so new scans pick naps2-scan (Done)
- XAS-024 — Persist scanner type (simplex/duplex) in naps2-scan config (Done)

**Acceptance Criteria**:
- [x] `plugins/xxthunder-paperless-skills/` exists with valid `plugin.json`
- [x] Marketplace lists the plugin as installable
- [ ] All three child skills present and working
- [x] `~/.claude/skills/simplex-merge/` removed (migration complete)
- [ ] End-to-end smoke test: scan a document, OCR it, merge if needed, propose filename, auto-file it
